import MxologistApp from "@/components/mxologist/MxologistApp";

// The full Mxologist experience — landing + four in-app screens — runs as a
// single client-side state machine recreated from the design handoff.
export default function Home() {
  return <MxologistApp />;
}
