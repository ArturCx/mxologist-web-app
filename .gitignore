node_modules/
dist/
.env
.env.local
.next/
*.log
*.tsbuildinfo
.turbo/
packages/database/generated/

# Agent tooling (local-only: vendored skills + machine-specific settings)
.agents/
.claude/
skills-lock.json

# Database data tooling — manual one-off backfill/import scripts and their
# source-data JSON. Not used by the deployed app (which relies on the Prisma
# client + migrations); kept local-only so the data isn't exposed on GitHub.
packages/database/scripts/